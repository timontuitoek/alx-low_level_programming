#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always 0.
 */

void swap_int(int *a, int *b)
{
	_putchar("values for a and b\n");
	scanf("%d%d", &a, &b);

	swap(&a, &b);
	_putchar("values for a and b\n");


	a = 98;
	b = 42;
}
