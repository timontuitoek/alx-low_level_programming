#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer
 * @i: integer input
 * Return: Absolute value of i
 */
int _abs(int i)
{
	if (i > 0)
	return (i);
	else if (i < 0)
	return (-i);
	else
	return (0);
}
