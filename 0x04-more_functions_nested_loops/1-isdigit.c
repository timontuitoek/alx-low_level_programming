#include "main.h"

/**
 * _isdigit - checks the dig
 *
 * @c: integer value of c
 *
 * Return: 1 if is between range else 0
 */

int _isdigit(int c)
{
	c = 0;

	if (c <= 9 && c++)
	{
	return (1);
	}
	else
	{
	return (0);
	}
}
